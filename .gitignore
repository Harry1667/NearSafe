# macOS
.DS_Store
._*

# Xcode / Swift build 產物
.build/
build/
DerivedData/
xcuserdata/

# fastlane 打包/簽章產物：.ipa、dSYM、佈建描述檔、憑證檔（每次打包重新產生；憑證私鑰絕不入庫）
*.mobileprovision
*.p12
*.cer
*.certSigningRequest

# 本機審查截圖（session 產物，不入庫）
.audit/
.gstack/

# 編輯器工作區檔（含機器特定路徑）
*.code-workspace

# 爬蟲執行產物（每次執行重新生成，不入庫）
tools/news-crawler/latest.json
tools/news-crawler/seen.json

# APNs 私鑰絕不進 git（外洩＝任何人可對使用者發推播）
*.p8

# Claude Code 本機設定（settings.local.json 是個人權限清單）
.claude/

# 黑客松提交工具產物（比賽已結束）
.scc/

# 根目錄的臨時截圖與 Demo 影片（正式素材放 website/ 或 design/）
/*.png
/*.mp4

# Firebase 服務帳戶私鑰（機密），只存伺服器與本機機密夾，絕不進 git
*firebase-adminsdk*.json
_fcm_service_account.json
